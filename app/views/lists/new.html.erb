<h1>New list</h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit 'Create', class: 'btn btn-success' %>
<% end %>

<%= link_to 'Back', lists_path, class: "btn btn-primary" %></td>
