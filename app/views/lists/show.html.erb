<%# voltar aqui neste item para colocar tudo junto %>

<h2><%= @list.name %></h2>

<p id="notice"><%= notice %></p>
<p>
  <strong>Details</strong>
  <%= @list.movies.each do |movie| %>
    <%= movie.title %>

  <% end %>
</p>


<%= link_to 'Back', lists_path, class: "btn btn-primary" %></td>
<%= link_to 'New bookmark', new_list_bookmark_path(@list), class: "btn btn-primary" %></td>
