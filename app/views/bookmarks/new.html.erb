<h1>New bookmark</h1>
<%# parei nesta etapa do exercício, não está buscando as associações %>
<%= simple_form_for @bookmark do |f| %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Back', list_path(@list), class: "btn btn-primary" %></td>
